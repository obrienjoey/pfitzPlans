# 02 — Render weekly volume ranges in WeekCard header

**What to build:**
Update `WeekCard` component header to display the formatted weekly volume range (`displayTotal.formatted`) alongside the unit string instead of assuming a single number.

**Blocked by:** 01 — Define WeeklyVolume domain model & update calculateWeeklyVolume

**Status:** ready-for-agent

- [ ] `WeekCard` updated to use `displayTotal.formatted`
- [ ] Header displays single values (e.g. `85 km`) or range values (e.g. `82.9 - 84.5 km`) seamlessly
- [ ] UI layout and styling remain crisp and visually balanced
