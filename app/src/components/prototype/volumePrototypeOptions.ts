// PROTOTYPE — throwaway (issue #2). DEV-only WeekCard volume variant keys.
// Imported by WeekCardVolumePrototype.tsx and PlanViewer.tsx. Delete with the prototype.
export const VOLUME_PROTOTYPE_OPTIONS = [
    { key: 'prod', label: 'current' },
    { key: 'vol-ledger', label: 'ledger column + spread' },
    { key: 'vol-bracket', label: 'interval bracket row' },
    { key: 'vol-stamp', label: 'volume stamp chip' },
];
